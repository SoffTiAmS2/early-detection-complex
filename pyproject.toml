[project]
name = "early-detection-complex"
version = "0.1.0"
description = "Распределённый комплекс раннего обнаружения на основе honeypot"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
edc-center = "center.main:main"

[tool.compileall]
quiet = 1
